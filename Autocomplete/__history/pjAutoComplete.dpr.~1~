program pjAutoComplete;

uses
  Vcl.Forms,
  uFrmPrincipal in 'Autocomplete\uFrmPrincipal.pas' {Form1},
  CadastroClientes in 'Autocomplete\classes\CadastroClientes.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
